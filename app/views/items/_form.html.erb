<%= form_for [user, user.items.new], remote: true do |f| %>
    <div class="form-item">
      <%= f.text_field :name, class: 'form-field' %>
    </div>
    <div class="form-item">
      <%= f.submit "Save", class: 'btn btn-border blue' %>
    </div>
<% end %>
